% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect-roles.R
\name{detect_roles}
\alias{detect_roles}
\title{Detect data roles for each column}
\usage{
detect_roles(data, profile = NULL)
}
\arguments{
\item{data}{A data frame.}

\item{profile}{Optional; a \code{dataganger_profile} object from
\code{\link[=profile_data]{profile_data()}}. If \code{NULL} (the default), profiling is performed
internally.}
}
\value{
An S3 object of class \code{dataganger_roles}, a tibble with columns:
\describe{
\item{variable}{Column name.}
\item{class}{R class of the column.}
\item{recommended_role}{Role detected by heuristic.}
\item{user_role}{User-supplied override (initially \code{NA}).}
\item{simulation}{How the column is treated during synthesis.}
\item{reason}{Justification for the recommended role.}
\item{identifies}{Whether the column points to a person: \code{"none"}, \code{"combination"}, or \code{"direct"}.}
\item{sensitive}{Logical flag for whether the column is sensitive if revealed.}
\item{user_identifies}{User-supplied override for \code{identifies} (initially \code{NA}).}
\item{user_sensitive}{User-supplied override for \code{sensitive} (initially \code{NA}).}
\item{disclosure_role}{Disclosure role. \code{NA} (unselected) is the
conservative default whenever detection is not confident; the user must
choose a role before generating. \code{"direct"} and \code{"sensitive"} are the
only values auto-assigned (confident identifier / known-sensitive name).
\code{"quasi"} and \code{"none"} are user-assigned choices only.}
\item{disclosure_reason}{Justification for the auto-assigned disclosure role.}
}
}
\description{
Applies heuristic-based role detection to every column in a data frame.
Roles include a recommended synthesis role plus the two primary disclosure
axes used by the Configure step: whether a column points to a person
(\code{identifies}) and whether it is sensitive. The legacy single
\code{disclosure_role} value is retained as derived compatibility metadata for
existing synthesis/export/CLI paths.
}
\details{
Columns whose name matches a postal-code pattern (postal, zip, postcode,
plz, cep, pin_code) are flagged as \code{"postal code"} with a
quasi-identifier disclosure default.
}
\examples{
df <- data.frame(
  id   = 1:50,
  date = as.Date("2020-01-01") + 0:49,
  city = rep(c("Toronto", "Vancouver", "Montreal"), length.out = 50),
  cat  = factor(rep(letters[1:3], length.out = 50))
)
detect_roles(df)
}
