% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enforce-kanon.R
\name{enforce_kanon}
\alias{enforce_kanon}
\title{Enforce k-anonymity on a synthetic dataset (output guarantee)}
\usage{
enforce_kanon(synthetic, roles, k = 5, max_steps = 6L, max_suppress_frac = 0.2)
}
\arguments{
\item{synthetic}{A synthetic data frame.}

\item{roles}{A roles object/data frame with \code{variable} + \code{disclosure_role}.}

\item{k}{Minimum cell size (default 5).}

\item{max_steps}{Maximum coarsening iterations (default 6).}

\item{max_suppress_frac}{Feasibility backstop. If satisfying \code{k} over the
quasi-identifier set would require blanking more than this fraction of
rows, k-anonymity is treated as infeasible for the chosen quasi-identifier
(QI) set: the coarsening and suppression steps are \emph{not} applied, the
synthetic output is returned populated, and a warning explains that no
k-anonymity protection was applied to that output. Default 0.2.}
}
\value{
The shaped \code{synthetic} data frame, with an attribute \code{kanon}
recording the achieved state (\code{smallest_cell}, \code{suppressed_cells},
\code{suppressed_rows}, \code{suppressed_row_frac}, \code{qi_cols}, \code{k}, \code{infeasible}).
\code{suppressed_rows}/\code{suppressed_row_frac} count actual blanked rows across
the QI columns -- distinct from \code{suppressed_cells}, which counts the
number of distinct QI combinations folded into suppression. The two can
differ a lot: reaching k can require absorbing a few whole neighbouring
cells (suppression works at cell granularity, not row granularity), and
a handful of small cells sitting next to one dominant cell can end up
suppressing most or all of a QI column even though only a few original
cells were actually below k.
}
\description{
Shapes the synthetic output so that no quasi-identifier combination appears
in fewer than \code{k} records. Direct identifiers are removed. Quasi-identifiers
are coarsened step-by-step and any residual cell still below \code{k} has its QI
values blanked (\code{NA}). Operates on the output only.
}
