% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export-diagnostic.R
\name{export_diagnostic_package}
\alias{export_diagnostic_package}
\title{Export a Lens-compatible diagnostic schema for a dataset}
\usage{
export_diagnostic_package(
  data,
  path,
  roles = NULL,
  profile = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{data}{A data frame to describe.}

\item{path}{Output path for the JSON file.}

\item{roles}{Optional; a \code{dataganger_roles} object from
\code{\link[=detect_roles]{detect_roles()}}. Computed internally if \code{NULL}.}

\item{profile}{Optional; a \code{dataganger_profile} object from
\code{\link[=profile_data]{profile_data()}}. Computed internally if \code{NULL}.}

\item{overwrite}{Logical. When \code{FALSE} (the default), aborts if
\code{path} already exists.}
}
\value{
Invisibly, the written JSON path.
}
\description{
Profiles a data frame and writes a \code{diagnostic_view.json} describing
column roles, sensitivity, and exposure levels. Does not synthesise data.
Intended for agent pre-inspection and Lens ingestion.
}
\examples{
dat <- data.frame(age = c(34, 29, 41), grp = c("a", "b", "c"))
export_diagnostic_package(dat, path = tempfile(fileext = ".json"))
}
