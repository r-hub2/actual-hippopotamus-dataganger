% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export-synthetic.R
\name{export_synthetic}
\alias{export_synthetic}
\title{Export a synthetic data bundle}
\usage{
export_synthetic(
  synthetic,
  original = NULL,
  comparison = NULL,
  privacy = NULL,
  path,
  format = c("zip", "dir"),
  sanitize_for_spreadsheets = TRUE,
  purpose = NULL,
  roles = NULL,
  include_original_names = NULL,
  fail_on_exact_match = FALSE,
  include_report = TRUE,
  kanon_acknowledged = FALSE,
  exact_match_acknowledged = FALSE,
  include_dictionary = TRUE,
  code_readiness = NULL,
  compact = FALSE,
  overwrite = FALSE
)
}
\arguments{
\item{synthetic}{A synthetic data frame, typically from \code{\link[=synthesize_data]{synthesize_data()}}.}

\item{original}{Optional original data frame. When provided, used for the
data dictionary, comparison fallback, privacy fallback, and exact-row guard.}

\item{comparison}{Optional \code{dataganger_comparison} object. If \code{NULL} and
\code{original} is supplied, \code{\link[=compare_synthetic]{compare_synthetic()}} is run automatically.}

\item{privacy}{Optional \code{dataganger_privacy_check} object. If \code{NULL} and
\code{original} is supplied, \code{\link[=privacy_check]{privacy_check()}} is run automatically at the post
stage.}

\item{path}{Output path. Required. For \code{format = "zip"}, this is the archive
path. For \code{format = "dir"}, this is the output directory.}

\item{format}{Character. One of \code{"zip"} or \code{"dir"}.}

\item{sanitize_for_spreadsheets}{Logical. When \code{TRUE} (the default),
character-like cells beginning with \code{=}, \code{+}, \code{-}, or \code{@} after leading
whitespace are prefixed with a single quote before CSV export.}

\item{purpose}{Optional purpose label for README text. Defaults to the
purpose recorded in \code{attr(synthetic, "spec")} when available.}

\item{roles}{Optional recorded role decisions as a \code{dataganger_roles} data
frame. When supplied, the export bundle includes the exact column
decisions needed to reproduce the same synthetic output.}

\item{include_original_names}{Logical or \code{NULL}. Controls whether the human
guide and manifest recipe preserve original variable names. When \code{NULL},
this defaults to \code{TRUE} unless \code{name_strategy = "dictionary_only"}, in
which case it defaults to \code{FALSE}.}

\item{fail_on_exact_match}{Logical. When \code{TRUE}, abort export if exact-row
matches are detected for \code{nrow(original) >= 20}. When \code{FALSE} (the
default), exact-row matches are recorded in the privacy report and
manifest, and a warning is emitted instead.}

\item{include_report}{Logical. When \code{TRUE} (the default), write
\code{human/comparison_report.html}. If \code{rmarkdown}/\code{knitr} are unavailable,
the report is skipped with a message instead of an error.}

\item{kanon_acknowledged}{Logical. Records whether a human explicitly
acknowledged exporting a bundle whose k-anonymity backstop was infeasible.
Interactive UI export uses this to clear the bundle blocker; non-interactive
exports leave it \code{FALSE}.}

\item{exact_match_acknowledged}{Logical. Records whether a human explicitly
acknowledged exporting a bundle in which synthetic rows reproduce real
records verbatim, including values marked sensitive. Interactive UI export
sets this only when the user ticks the override; non-interactive exports
leave it \code{FALSE}.}

\item{include_dictionary}{Deprecated, ignored.}

\item{code_readiness}{Optional \code{dataganger_code_readiness} object from
\code{\link[=check_code_readiness]{check_code_readiness()}}. When supplied, writes
\code{agent/code_readiness_report.json} into the bundle.}

\item{compact}{Deprecated, ignored.}

\item{overwrite}{Logical. When \code{FALSE} (the default), existing output paths
are refused.}
}
\value{
Invisibly, the written bundle path.
}
\description{
Writes a reviewable export bundle containing the synthetic data, a human
guide, an optional comparison report, a combined agent recipe, the packaged
agent instructions, and a manifest. By default the bundle is written as a
zip archive.
}
\examples{
dat <- data.frame(
  age = 21:70,
  score = seq(10, 59),
  grp = rep(LETTERS[1:5], each = 10)
)
spec <- synth_spec(purpose = "demo", seed = 1, engine = "internal")
syn <- synthesize_data(dat, spec)
export_synthetic(syn, path = tempfile(fileext = ".zip"), include_report = FALSE)
}
