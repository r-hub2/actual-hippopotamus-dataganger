% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make-agent-bundle.R
\name{make_agent_bundle}
\alias{make_agent_bundle}
\title{Create a one-command agent-ready bundle from a raw data file}
\usage{
make_agent_bundle(
  file,
  out,
  purpose = "development",
  seed = NULL,
  overwrite = FALSE,
  ...
)
}
\arguments{
\item{file}{Path to the input data file. Passed to \code{\link[=read_input]{read_input()}}.}

\item{out}{Path for the output zip file.}

\item{purpose}{Synthesis purpose preset. Defaults to \code{"development"}.
See \code{\link[=synth_spec]{synth_spec()}} for valid values.}

\item{seed}{Optional integer random seed for reproducible synthesis.}

\item{overwrite}{Logical. When \code{FALSE} (the default), aborts if
\code{out} already exists.}

\item{...}{Additional arguments passed to \code{\link[=read_input]{read_input()}} only
(e.g. \code{encoding}, \code{sheet}).}
}
\value{
Invisibly, the written bundle path.
}
\description{
Reads a data file, profiles it, detects column roles, synthesizes data, and
exports a zip bundle suitable for passing to an AI agent.
}
\examples{
dat <- data.frame(
  age = c(24, 28, 35, 41),
  score = c(88, 91, 84, 95),
  grp = c("A", "A", "B", "B")
)
path <- tempfile(fileext = ".csv")
readr::write_csv(dat, path)
make_agent_bundle(
  file = path,
  out = tempfile(fileext = ".zip"),
  purpose = "demo",
  seed = 1
)
}
