% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare-synthetic.R
\name{plot_comparison}
\alias{plot_comparison}
\title{Plot comparison summaries}
\usage{
plot_comparison(comparison)
}
\arguments{
\item{comparison}{A \code{dataganger_comparison} object from
\code{\link[=compare_synthetic]{compare_synthetic()}}.}
}
\value{
Invisibly, a list with two \code{ggplot} objects: \code{numeric} and
\code{categorical}. Each is \code{NULL} if no columns of that type exist.
}
\description{
Produces two bar charts comparing original and synthetic data:
standardized differences for numeric columns and total variation
distance for categorical columns. Requires \code{ggplot2} (Suggests).
}
\examples{
dat <- data.frame(x = 1:10, y = letters[1:10])
spec <- synth_spec(purpose = "demo")
syn <- synthesize_data(dat, spec)
cmp <- compare_synthetic(dat, syn)
if (requireNamespace("ggplot2", quietly = TRUE)) {
  plot_comparison(cmp)
}
}
