% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/privacy-check.R
\name{privacy_check}
\alias{privacy_check}
\title{Run disclosure-risk privacy checks}
\usage{
privacy_check(
  original,
  synthetic = NULL,
  roles = NULL,
  stage = c("pre", "post"),
  spec = NULL
)
}
\arguments{
\item{original}{The original data frame.}

\item{synthetic}{Optional; the synthetic data frame (required for
\code{stage = "post"}).}

\item{roles}{Optional; a \code{dataganger_roles} object from \code{\link[=detect_roles]{detect_roles()}}.
Recommended for pre-stage flag detection. When omitted, fallback name/type heuristics are used.}

\item{stage}{Character. \code{"pre"} or \code{"post"}.}

\item{spec}{Optional; a \code{dataganger_spec} object. When provided at
\code{stage = "post"}, cross-checks that synthesis parameters were applied
(e.g. date coarsening, ID removal).}
}
\value{
An S3 object of class \code{dataganger_privacy_check}, a tibble with
columns \code{variable}, \code{flag}, \code{severity}, \code{stage}, and \code{recommendation}.
}
\description{
Scans original and (optionally) synthetic data for disclosure-risk flags.
Supports two stages: \code{"pre"} (before synthesis, requires only the
original dataset and roles) and \code{"post"} (after synthesis, requires both
original and synthetic).
}
\examples{
df <- data.frame(id = 1:50, x = rnorm(50), city = rep("Toronto", 50))
roles <- detect_roles(df)
privacy_check(df, roles = roles, stage = "pre")
}
