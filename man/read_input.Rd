% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-input.R
\name{read_input}
\alias{read_input}
\title{Read a data file into a tibble}
\usage{
read_input(file, sheet = NULL, encoding = NULL, col_select = NULL, ...)
}
\arguments{
\item{file}{Path to the data file.}

\item{sheet}{For Excel files, the sheet name or index to read. Passed to
\code{\link[readxl:read_excel]{readxl::read_excel()}}. Ignored for non-Excel formats.}

\item{encoding}{Character encoding for CSV files (e.g. \code{"UTF-8"},
\code{"latin1"}). Passed as \code{readr::locale(encoding = encoding)}. Ignored when
reading non-CSV formats or when the caller already supplies a \code{locale}
argument in \code{...}.}

\item{col_select}{Optional character vector of column names to keep. For CSV
files the selection is passed directly to \code{\link[readr:read_csv]{readr::read_csv()}} so excluded
columns are never parsed. For other formats columns are subset after
reading.}

\item{...}{Additional arguments passed to the underlying reader
(\code{readr::read_csv()}, \code{readxl::read_excel()}, \code{haven::read_sas()}, or
\code{haven::read_xpt()}).}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}}. SAS/XPT imports preserve \code{haven_labelled}
vectors as-is.
}
\description{
Reads CSV, Excel (\code{.xlsx}, \code{.xls}), SAS (\code{.sas7bdat}), and XPT (\code{.xpt})
files into a tibble. Dispatches on file extension.
}
\examples{
path <- tempfile(fileext = ".csv")
readr::write_csv(data.frame(id = 1:3, grp = c("a", "b", "c")), path)
read_input(path)
}
