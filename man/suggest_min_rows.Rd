% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/suggest-min-rows.R
\name{suggest_min_rows}
\alias{suggest_min_rows}
\title{Suggest a sufficient synthetic row count}
\usage{
suggest_min_rows(
  profile,
  roles = NULL,
  data = NULL,
  k = 5L,
  threshold = 1000L,
  cap = 5000L
)
}
\arguments{
\item{profile}{A \code{dataganger_profile} from \code{\link[=profile_data]{profile_data()}}.}

\item{roles}{Optional; a \code{dataganger_roles} object. When provided together
with \code{data}, the coverage computation is filtered to only the columns whose
effective role is synthesizable (excludes alphanumeric IDs, free text, and
user-excluded columns).}

\item{data}{Optional; the original data frame. When provided alongside
\code{roles}, coverage is recomputed on the filtered column subset so that the
suggestion reacts to role changes on the Configure page.}

\item{k}{Reserved for a future k-anonymity-style cell-size floor; unused by
the current coverage rule.}

\item{threshold}{Row count at or above which a reduction is suggested.}

\item{cap}{Maximum suggested row count from combination coverage.}
}
\value{
A list with:
\describe{
\item{n}{Suggested integer row count.}
\item{rationale}{Human-readable explanation.}
\item{original_n}{Original row count.}
\item{combination_count}{Observed category-combination count (or \code{NA}).}
\item{floor}{Per-column distinct floor used (or \code{NA}).}
\item{capped}{\code{TRUE} if the cap bound the suggestion.}
\item{reduced}{\code{TRUE} if the suggestion is below the original count.}
}
}
\description{
Given a \code{\link[=profile_data]{profile_data()}} profile (which carries cross-column coverage
information), suggests how many rows to synthesize so that the synthetic
data can still represent every category combination and every category
level observed in the original data, without blindly matching a large
original row count.
}
\details{
The rule (coverage-based) is:
\itemize{
\item For small inputs (fewer than \code{threshold} rows, default 1000) the
original row count is kept --- there is nothing to gain from reducing.
\item Otherwise the suggestion is the number of observed cross-column
category combinations, capped at \code{cap} (default 5000) to avoid
suggesting millions of rows on wide data, and floored at the largest
per-column distinct count so every level remains representable. The
suggestion never exceeds the original row count.
}

Continuous columns are covered by preserving their min/max (already handled
by the synthesis engine); they do not raise the suggested count.
}
\examples{
p <- profile_data(datasets::iris)
suggest_min_rows(p)
}
