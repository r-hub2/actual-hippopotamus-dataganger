% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synthesize-data.R
\name{synthesize_data}
\alias{synthesize_data}
\title{Synthesize a data double}
\usage{
synthesize_data(data, spec, roles = NULL, engine = NULL)
}
\arguments{
\item{data}{A data frame to synthesize from.}

\item{spec}{A \code{dataganger_spec} object from \code{\link[=synth_spec]{synth_spec()}}.}

\item{roles}{Optional; a \code{dataganger_roles} object from \code{\link[=detect_roles]{detect_roles()}}.
Informs column treatment but does not override the spec.}

\item{engine}{Character or \code{NULL}. Engine to use: \code{"auto"}, \code{"internal"},
\code{"marginal"} (alias for \code{"internal"}), or \code{"synthpop"}. When
\code{NULL}, defaults to \code{spec$engine} or derives from
\code{spec$preserve_correlations}.}
}
\value{
An S3 object of class \code{dataganger_synthetic}, a tibble with
attributes \code{spec}, \code{original_dims}, \code{seed_used}, and \code{generated_at}.
Categorical and other non-numeric text columns are returned as plain
\code{character}; numeric, logical, date, and datetime columns retain their
corresponding types.
}
\description{
Creates a synthetic copy of a dataset using the specified specification
and engine. The internal engine supports schema-only (Level 1) and
marginal (Level 2) synthesis. The optional synthpop engine is used for
objectives that request moderate or high relationship preservation.
}
\section{Disabling synthpop}{

Set \code{options(dataganger.disable_synthpop = TRUE)} to steer
auto-derived synthesis onto the internal engine even when synthpop is
installed. This is intended for environments where a synthpop synthesis
is undesirable or can hang unattended (for example continuous
integration). An explicit \code{engine = "synthpop"} request is still
honoured; only objective-derived routing is affected.
}

\examples{
dat <- data.frame(x = 1:5, y = letters[1:5])
spec <- synth_spec(purpose = "demo")
syn <- synthesize_data(dat, spec)
}
